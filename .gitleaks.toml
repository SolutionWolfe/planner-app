# gitleaks configuration. config.js holds public configuration whose format looks like a token
# to the scanner; everything else is scanned.
title = "planner-app"

[extend]
useDefault = true

[allowlist]
paths = ['''^config\.js$''']
